import React from 'react';
import { Button, Flex } from 'antd';
const Tools = () => (
    <>
        <Flex gap="small" wrap="wrap">
            <Button type="primary">新建连接</Button>
            <Button type="primary">新建连接</Button>
            <Button type="primary">新建连接</Button>
            <Button type="primary">新建连接</Button>
        </Flex>
    </>
);
export default Tools;